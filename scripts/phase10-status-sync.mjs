import { readFile, writeFile } from "node:fs/promises";

async function replaceOnce(file, oldText, newText) {
  const text = await readFile(file, "utf8");
  if (text.includes(newText)) return false;
  const count = text.split(oldText).length - 1;
  if (count !== 1) throw new Error(`${file}: expected exactly one status block, found ${count}.`);
  await writeFile(file, text.replace(oldText, newText));
  return true;
}

const readmeOld = `- repeatable performance/load baselines covering public and authenticated reads, bounded mutation contention, runtime RSS/file-descriptor/thread behavior and post-spike liveness/recovery.\n`;
const readmeNew = `${readmeOld}- reproducible fresh-clone demo bootstrap with a versioned npm lockfile and no mandatory external infrastructure;\n- provider-neutral self-host packaging around the real Next.js standalone runtime, with EN/ES deployment guidance and blocking HTTP/static-asset smoke.\n`;
const readmeStatusOld = `**Phase 8 — External integrations is COMPLETE. The Phase 9 — Production hardening engineering baseline is COMPLETE across Phase 9A production security, Phase 9B critical validation, Phase 9C observability/recovery/privileged-audit hardening and Phase 9D privacy/regulatory/accessibility/performance readiness. Phase 10 — Open-source productisation is NEXT.**`;
const readmeStatusNew = `**Phase 8 — External integrations and the Phase 9 — Production hardening engineering baseline are COMPLETE. Phase 10 — Open-source productisation is IN PROGRESS: 10.1 reproducible fresh-clone/demo bootstrap and 10.2 provider-neutral self-host standalone deployment are COMPLETE; 10.3 extension contracts/reference adapters is NEXT.**`;

const readmeEsOld = `- baselines repetibles de rendimiento/carga para lecturas públicas y autenticadas, contención acotada de mutaciones, comportamiento runtime de RSS/descriptores/threads y liveness/recuperación post-pico.\n`;
const readmeEsNew = `${readmeEsOld}- bootstrap demo reproducible desde clon limpio con lockfile npm versionado y sin infraestructura externa obligatoria;\n- empaquetado self-host provider-neutral sobre el runtime standalone real de Next.js, con guía EN/ES y smoke bloqueante HTTP/assets.\n`;
const readmeEsStatusOld = `**La Fase 8 — Integraciones externas está COMPLETADA. El baseline de ingeniería de la Fase 9 — Endurecimiento productivo está COMPLETADO: Fase 9A de seguridad productiva, Fase 9B de validación crítica, Fase 9C de observabilidad/recuperación/auditoría privilegiada y Fase 9D de privacidad/regulación/accesibilidad/rendimiento. La Fase 10 — Productización open-source es la SIGUIENTE.**`;
const readmeEsStatusNew = `**La Fase 8 — Integraciones externas y el baseline de ingeniería de la Fase 9 — Endurecimiento productivo están COMPLETADOS. La Fase 10 — Productización open-source está EN CURSO: 10.1 bootstrap demo reproducible desde clon limpio y 10.2 despliegue self-host standalone provider-neutral están COMPLETADOS; 10.3 contratos de extensión/adapters de referencia es la SIGUIENTE.**`;

const roadmapOld = `# Phase 10 — Open-source productisation — NEXT\n\n- production environment documentation;\n- clean demo seed/setup;\n- fresh-clone install/deployment guide;\n- reference adapters and extension contracts;\n- versioned releases/migrations;\n- contribution templates and public API/extension docs;\n- optional Docker/self-host example;\n- trademark/branding policy;\n- proprietary Kairoseth/customer adapters outside the MIT core where appropriate.`;
const roadmapNew = `# Phase 10 — Open-source productisation — IN PROGRESS\n\n### 10.1 — Reproducible fresh-clone/demo bootstrap — COMPLETE\n- versioned npm 11 lockfile makes \`npm ci\` work from a clean clone;\n- safe \`.env.demo.example\` and non-destructive \`npm run setup:demo\` require no MongoDB, SMTP, PSP, CRM, ERP or supplier account;\n- EN/ES getting-started guides preserve the provider-neutral Kairoseth boundary;\n- blocking clean-checkout build/start/HTTP smoke proves the public onboarding contract.\n\n### 10.2 — Provider-neutral self-host standalone deployment — COMPLETE\n- \`npm run package:standalone\` turns Next.js \`output: standalone\` into a transportable runtime with \`.next/static\` and \`public\` assets;\n- blocking self-host CI starts the real \`.next/standalone/server.js\` and validates public pages, liveness and a public asset without external infrastructure;\n- the runtime resource baseline now measures that same standalone process instead of the Next.js CLI;\n- deployment guidance is bilingual and covers build-time/runtime configuration, live readiness, reverse proxy/TLS, MongoDB, payments, workers, secrets, immutable releases and rollback;\n- \`travel.kairoseth.com\` remains a reference/commercial deployment, not a dependency of the MIT core.\n\n### 10.3 — Extension contracts and reference adapters — NEXT\n- consolidate public extension points and stable provider-neutral adapter contracts;\n- add contributor-facing examples for implementing adapters without granting downstream systems authority over core booking/payment state;\n- define compatibility/versioning expectations for extension contracts before broader ecosystem work.\n\nLater Phase 10 slices will cover release/migration conventions, contribution templates and trademark/branding policy while keeping proprietary Kairoseth/customer adapters outside the MIT core where appropriate.`;

const roadmapOrderOld = `10    Open-source productisation / release — NEXT`;
const roadmapOrderNew = `10.1  Fresh-clone/demo bootstrap — COMPLETE\n  ↓\n10.2  Self-host standalone deployment — COMPLETE\n  ↓\n10.3  Extension contracts/reference adapters — NEXT`;

const roadmapEsOld = `# Fase 10 — Productización open-source — SIGUIENTE\n\n- documentación de entorno productivo;\n- seed/setup demo limpio;\n- instalación/despliegue desde clon limpio;\n- adapters de referencia y contratos de extensión;\n- releases/migraciones versionadas;\n- templates de contribución y documentación pública API/extensiones;\n- ejemplo opcional Docker/self-host;\n- política de marca/trademark;\n- adapters propietarios Kairoseth/cliente fuera del core MIT cuando corresponda.`;
const roadmapEsNew = `# Fase 10 — Productización open-source — EN CURSO\n\n### 10.1 — Bootstrap demo reproducible desde clon limpio — COMPLETADO\n- lockfile npm 11 versionado para que \`npm ci\` funcione desde un clon limpio;\n- \`.env.demo.example\` seguro y \`npm run setup:demo\` no destructivo sin exigir MongoDB, SMTP, PSP, CRM, ERP ni cuentas de proveedor;\n- guías EN/ES de primeros pasos preservan la frontera provider-neutral con Kairoseth;\n- smoke bloqueante desde checkout limpio demuestra build, arranque y HTTP del contrato público.\n\n### 10.2 — Despliegue self-host standalone provider-neutral — COMPLETADO\n- \`npm run package:standalone\` convierte \`output: standalone\` de Next.js en un runtime transportable con \`.next/static\` y assets de \`public\`;\n- CI self-host bloqueante arranca el \`.next/standalone/server.js\` real y valida páginas públicas, liveness y un asset sin infraestructura externa;\n- el baseline de recursos runtime mide ahora ese mismo proceso standalone en lugar del CLI de Next.js;\n- guía de despliegue bilingüe cubre configuración build/runtime, readiness live, proxy inverso/TLS, MongoDB, pagos, workers, secretos, releases inmutables y rollback;\n- \`travel.kairoseth.com\` sigue siendo despliegue comercial/de referencia, no dependencia del core MIT.\n\n### 10.3 — Contratos de extensión y adapters de referencia — SIGUIENTE\n- consolidar puntos públicos de extensión y contratos provider-neutral estables;\n- añadir ejemplos para contribuidores que implementen adapters sin otorgar a downstream autoridad sobre reservas/pagos del core;\n- definir expectativas de compatibilidad/versionado de contratos antes de ampliar el ecosistema.\n\nLos siguientes bloques de Fase 10 cubrirán convenciones de releases/migraciones, templates de contribución y política de marca/trademark manteniendo adapters propietarios Kairoseth/cliente fuera del core MIT cuando corresponda.`;

const roadmapEsOrderOld = `10    Productización open-source / release — SIGUIENTE`;
const roadmapEsOrderNew = `10.1  Bootstrap demo desde clon limpio — COMPLETADO\n  ↓\n10.2  Despliegue self-host standalone — COMPLETADO\n  ↓\n10.3  Contratos de extensión/adapters de referencia — SIGUIENTE`;

const changes = [];
changes.push(await replaceOnce("README.md", readmeOld, readmeNew));
changes.push(await replaceOnce("README.md", readmeStatusOld, readmeStatusNew));
changes.push(await replaceOnce("README.es.md", readmeEsOld, readmeEsNew));
changes.push(await replaceOnce("README.es.md", readmeEsStatusOld, readmeEsStatusNew));
changes.push(await replaceOnce("ROADMAP.md", roadmapOld, roadmapNew));
changes.push(await replaceOnce("ROADMAP.md", roadmapOrderOld, roadmapOrderNew));
changes.push(await replaceOnce("ROADMAP.es.md", roadmapEsOld, roadmapEsNew));
changes.push(await replaceOnce("ROADMAP.es.md", roadmapEsOrderOld, roadmapEsOrderNew));
console.log(`Phase 10 status sync complete; changed=${changes.filter(Boolean).length}.`);
