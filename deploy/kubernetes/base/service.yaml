apiVersion: v1
kind: Service
metadata:
  name: open-travel-platform
  labels:
    app.kubernetes.io/name: open-travel-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: open-travel-platform
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
