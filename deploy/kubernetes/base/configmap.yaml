apiVersion: v1
kind: ConfigMap
metadata:
  name: open-travel-platform-runtime
  labels:
    app.kubernetes.io/name: open-travel-platform
data:
  NODE_ENV: production
  NEXT_TELEMETRY_DISABLED: "1"
  HOSTNAME: "0.0.0.0"
  PORT: "3000"
