name: Privacy retention policy

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: privacy-retention-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NEXT_PUBLIC_DATA_MODE: demo
      IDENTITY_MODE: disabled
      BOOKING_MODE: disabled
      OPERATIONS_MODE: disabled
      KTRAVEL_DEPLOYMENT_PROFILE: demo
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 24
          package-manager-cache: false

      - name: Resolve dependency lock
        run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund

      - name: Clean install from resolved lock
        run: npm ci --no-fund

      - name: Privacy retention invariant check
        run: npm run check:privacy-retention-policy

      - name: Privacy retention policy test
        run: npm run test:privacy-retention-policy

      - name: Type check
        run: npm run typecheck

      - name: Production build
        run: npm run build
